services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: armory:latest
    container_name: armory
    restart: unless-stopped
    ports:
      - "${PORT:-8088}:${PORT:-8088}"
    volumes:
      - ./data:/app/data
      - ./projectdocs.db:/app/projectdocs.db
    env_file:
      - .env
